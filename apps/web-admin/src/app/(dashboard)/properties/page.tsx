import { Building2 } from 'lucide-react';
import { PlaceholderPage } from '@/components/layout/PlaceholderPage';

export default function PropertiesPage() {
  return (
    <PlaceholderPage
      icon={Building2}
      title="Cơ sở & Phòng"
      description="Khai báo cơ sở, phòng vật lý và bookable unit (bán từng phòng hay nguyên căn), block bảo trì, gói giá theo giờ/ngày/tháng."
      plan="Sprint 2 · task 2.1–2.2"
    />
  );
}
