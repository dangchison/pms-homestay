/**
 * TODO(task 6.6): Service worker Workbox (docs/13 §4):
 *   - precache app shell
 *   - runtime cache GET (NetworkFirst, fallback cache khi offline)
 *   - ★ READ-CACHE ONLY: xem dữ liệu khi rớt mạng; MUTATION yêu cầu online
 *     (disable nút + OfflineBanner). KHÔNG có offline mutation queue —
 *     sync conflict với booking/occupancy là bài toán chưa cần ở MVP.
 *
 * File này chưa được wire vào build — Workbox setup nằm trọn trong task 6.6.
 */
export {};
