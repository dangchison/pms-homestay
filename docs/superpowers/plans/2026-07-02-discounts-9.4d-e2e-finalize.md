# Task 9.4d — e2e discounts + quote-with-discount + typecheck cuối (Wave-1 #4)

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Đóng (finalize) tính năng mã giảm giá/voucher — bổ sung ĐÚNG các lỗ hổng e2e còn thiếu so với summary 9.4d, chạy 1 spec sạch, typecheck LẦN CUỐI (bao gồm test/), cập nhật PROGRESS.md, và flag follow-up (FE + openapi).

**Architecture:** 9.4a (migration 0032 + shared-types), 9.4b (DiscountsService + redeem atomic + e2e phần lớn), 9.4c (wire quote + endpoint validate + e2e mở rộng) ĐÃ COMMIT. `apps/api/test/e2e/discounts.e2e-spec.ts` (653 dòng) HIỆN ĐÃ phủ gần như toàn bộ summary 9.4d. Task này KHÔNG viết lại từ đầu — nó là **verify + vá đúng phần thiếu + đóng**.

**Tech Stack:** NestJS + Vitest (pool forks, maxForks=2) + supertest + `pg` admin client (seed/cleanup trực tiếp) + Postgres RLS.

## Global Constraints

- **KHÔNG `rtk vitest`** (rtk làm chết vitest). Chạy e2e theo memory `run-be-e2e-locally`: `LOG_LEVEL=fatal`, đặt `env` khi exec vitest, wd không bền → dùng `(cd apps/api && …)`.
- **KHÔNG slug-swap** để test cross-tenant: luôn giữ token của người gọi (RLS ẩn tài nguyên tenant khác). Không đặt `X-Tenant-Slug` của tenant A rồi dùng token tenant B.
- Migration đã xong (0032, 0033) — **task này KHÔNG thêm migration**. Không đụng schema/API cũ.
- Tiền = BIGINT + `roundVnd()`; số tiền giảm tính DUY NHẤT ở `evaluate` (service) — test chỉ assert, không tự tính lại công thức khác.
- Cleanup `afterAll` đúng thứ tự FK: `app.close()` **KHÔNG** gọi trước khi xoá DB — thực tế trong spec hiện tại `app?.close()` chạy **CUỐI** (sau `admin.end()`); GIỮ nguyên. FK: `quotes`/`bookings` xoá **trước** `discount_codes`, `discount_codes` xoá **trước** `tenants`.

---

## Bối cảnh đã có (đọc trước khi sửa — KHÔNG lặp lại)

`apps/api/test/e2e/discounts.e2e-spec.ts` đã có các nhóm:
- **CRUD qua HTTP** — tạo FIXED/PERCENT, refine biên (PERCENT>10000→400, FIXED<1→400), trùng CITEXT→409, list+getById, PATCH, PATCH đổi-type-lệch→422, soft-delete→biến khỏi list + `applyDiscount` NOT_FOUND + re-tạo cùng code, **ACCOUNTANT→403 create/list**, **cross-tenant tenant2 KHÔNG thấy mã tenant1 + getById/patch/delete→404** (dùng `otherToken`, KHÔNG slug-swap).
- **applyDiscount (service thuần)** — FIXED OK, FIXED cap theo subtotal, PERCENT roundVnd (1500/15% trên 333333→50000 half-away), PERCENT 100%, NOT_FOUND, INACTIVE, NOT_STARTED, EXPIRED, biên closed-interval, BELOW_MIN_ORDER, PROPERTY_NOT_ELIGIBLE, `applicable=[]` vs NULL, USAGE_LIMIT_REACHED.
- **GET /:code/validate** — parity với applyDiscount, mã không tồn tại→200 NOT_FOUND (KHÔNG 404), reason parity, gate `booking.read` STAFF→200 / **HOUSEKEEPER→403**, cross-tenant→200 NOT_FOUND, coerce subtotal, validation 400.
- **redemption qua createBookingTx** — used_count +1 CÙNG tx, mã cạn→409 DISCOUNT_EXHAUSTED + used_count không đổi + booking không tồn tại, soft-delete→409, quote KHÔNG có code_id→luồng cũ, max_uses NULL→không giới hạn.
- **end-to-end voucher (quote→booking)** — total netted + used_count +1 + KHÔNG 409 PRICE_CHANGED, booking-2 trên mã cạn→409, **replay cùng Idempotency-Key → used_count KHÔNG tăng lần 2**.

`apps/api/test/e2e/pricing-quote.e2e-spec.ts` đã có nhóm **quote WITH discount_code (9.4c)**: FIXED total-netted + DISCOUNT line âm + persist `discount_code_id`, PERCENT roundVnd, FIXED cap→total 0, mã không hợp lệ→422 DISCOUNT_NOT_APPLICABLE + KHÔNG persist quote, inactive→422. Cleanup đã xoá `discount_codes` sau `quotes`.

**Kết luận:** Chỉ còn **MỘT lỗ hổng test thật** (HOUSEKEEPER→403 trên endpoint QUẢN LÝ, không phải chỉ validate), phần còn lại là **verify + đóng**.

---

### Task 1: Vá lỗ hổng test — HOUSEKEEPER thiếu quyền QUẢN LÝ → 403

**Files:**
- Modify: `apps/api/test/e2e/discounts.e2e-spec.ts` (thêm 1 `it` vào `describe('CRUD discount_codes qua HTTP')`, cạnh test ACCOUNTANT→403, khoảng dòng 318–321)

**Interfaces (đã có, tái dùng — KHÔNG khai lại):**
- `hkToken: string` (dòng 42; HOUSEKEEPER tenant1, KHÔNG có `rate_plan.manage`).
- `dc = (path = '', tok = token)` → `request(http).post('/api/v1/discount-codes'+path).set(auth(tok))` (dòng 51).
- `auth = (tok = token)` → `{ Authorization: 'Bearer '+tok }` (dòng 50).
- `RUN`, `http` (đã có).

**Lý do (proxy CEO):** Summary 9.4d nêu rõ *"thiếu quyền quản lý (HOUSEKEEPER) → 403"*. Spec hiện tại CHỈ chứng minh HK→403 trên `validate` (gate `booking.read`) và ACCOUNTANT→403 trên quản lý (gate `rate_plan.manage`). HOUSEKEEPER cũng KHÔNG có `rate_plan.manage` → phải 403 trên create/list/getById/patch/delete. Đây là ma trận RBAC khác (housekeeper là vai thấp nhất) → cần chốt riêng, KHÔNG suy ra từ ACCOUNTANT.

- [ ] **Step 1: Thêm test HK→403 trên toàn bộ CRUD quản lý**

Chèn `it` sau test ACCOUNTANT→403 (ngay trước test cross-tenant, khoảng dòng 321):

```ts
it('HOUSEKEEPER (không rate_plan.manage) → 403 mọi endpoint QUẢN LÝ (create/list/get/patch/delete)', async () => {
  // HK có booking.read (validate 200) nhưng KHÔNG rate_plan.manage → chặn toàn bộ quản lý mã.
  await dc('', hkToken).send({ code: `HK-${RUN}`, discount_type: 'FIXED', discount_value: 1000 }).expect(403);
  await request(http).get('/api/v1/discount-codes').set(auth(hkToken)).expect(403);
  await request(http).get(`/api/v1/discount-codes/${fixedId}`).set(auth(hkToken)).expect(403);
  await request(http).patch(`/api/v1/discount-codes/${fixedId}`).set(auth(hkToken)).send({ is_active: false }).expect(403);
  await request(http).delete(`/api/v1/discount-codes/${fixedId}`).set(auth(hkToken)).expect(403);
});
```

Ghi chú: `fixedId` là biến `let` trong `describe('CRUD…')` (dòng 226) đã được gán ở test đầu tiên (tạo FIXED). Vì Vitest chạy các `it` tuần tự theo thứ tự khai báo, `fixedId` đã sẵn khi test này chạy. Nếu executor đặt test ở vị trí khác (không khuyến khích), phải bảo đảm sau test "tạo mã FIXED".

- [ ] **Step 2: Chạy riêng spec discounts (sạch, đúng run-be-e2e-locally)**

Run:
```bash
(cd apps/api && LOG_LEVEL=fatal pnpm exec vitest run test/e2e/discounts.e2e-spec.ts)
```
Expected: ALL PASS (bao gồm test HK-403 mới). KHÔNG dùng `rtk vitest`. KHÔNG `LOG_LEVEL=silent` (bị reject). Nếu transient IO (SMTP ECONNRESET/tx chậm do maxForks) → chạy lại 1 lần; chỉ điều tra khi fail lặp lại.

- [ ] **Step 3: Chạy thêm spec pricing-quote (đảm bảo nhóm voucher 9.4c vẫn xanh)**

Run:
```bash
(cd apps/api && LOG_LEVEL=fatal pnpm exec vitest run test/e2e/pricing-quote.e2e-spec.ts)
```
Expected: ALL PASS. (Không sửa file này; chạy để chốt không hồi quy.)

---

### Task 2: Verify coverage 9.4d (checklist đối chiếu — KHÔNG viết code nếu đã phủ)

**Files:** không sửa (trừ khi phát hiện thiếu — xem dưới).

Đối chiếu từng gạch đầu dòng của summary 9.4d với spec hiện có. Với MỖI mục, mở file và xác nhận có test tương ứng:

- [ ] **CRUD** → `describe('CRUD discount_codes qua HTTP')` ✅ (đã có).
- [ ] **FIXED giảm đúng số & cap không vượt subtotal** → `applyDiscount` "FIXED OK" + "FIXED cap" ✅.
- [ ] **PERCENT theo basis-point + roundVnd** → "PERCENT roundVnd 1500→50000" + pricing-quote "PERCENT 1500→75000" ✅.
- [ ] **min_order chưa đạt → không áp (reason MIN_ORDER)** → "BELOW_MIN_ORDER" ✅ (reason_code chuẩn tên `BELOW_MIN_ORDER`; summary viết tắt "MIN_ORDER").
- [ ] **ngoài valid window → không áp** → "NOT_STARTED" + "EXPIRED" + biên closed-interval ✅.
- [ ] **max_uses cạn → REJECT + used_count không vượt** → redemption "mã tại-giới-hạn → 409 + used_count không đổi" ✅.
- [ ] **applicable_property_ids không khớp → không áp** → "PROPERTY_NOT_ELIGIBLE" + "applicable=[]" ✅.
- [ ] **quote có discount_code → discount_vnd/total_vnd đúng** → pricing-quote nhóm 9.4c ✅.
- [ ] **thiếu code → hành vi cũ (discount 0, backward-compat)** → redemption "quote KHÔNG có discount_code_id → luồng cũ" + pricing-quote path không gửi mã (byte-identical) ✅.
- [ ] **tạo booking từ quote-có-mã → used_count +1 đúng 1 lần** → e2e-voucher "quote(code)→booking 201 + used_count +1" ✅.
- [ ] **idempotent, không double khi retry Idempotency-Key** → e2e-voucher "replay cùng Idempotency-Key → used_count KHÔNG tăng lần 2" ✅.
- [ ] **cross-tenant → RLS 404 (KHÔNG slug-swap)** → CRUD "cross-tenant tenant2 getById/patch/delete→404" (dùng `otherToken`) ✅. XÁC NHẬN không có `.set('X-Tenant-Slug', otherSlug)` kèm token tenant1 ở bất kỳ đâu (đã kiểm: chỉ dùng ở login).
- [ ] **thiếu quyền quản lý (HOUSEKEEPER) → 403** → **Task 1 vừa thêm** ✅.
- [ ] **Cleanup afterAll đúng thứ tự FK (app.close; discount_codes trước tenants)** → `afterAll` (dòng 191–221): xoá `quotes`/`bookings` trước `discount_codes`, `discount_codes` trước `tenants`, `admin.end()` rồi `app?.close()` cuối. ✅ GIỮ NGUYÊN.

**Nếu (và chỉ nếu) phát hiện một mục KHÔNG có test** → thêm `it` tối thiểu cho mục đó theo mẫu seedCode/applyDiscount đã có; nếu tất cả ✅ (dự kiến) → không sửa gì ở task này.

- [ ] **Step 1: Không có thay đổi code nếu mọi mục ✅.** Ghi lại (trong PR description) rằng đã đối chiếu đủ 15 mục.

---

### Task 3: Typecheck LẦN CUỐI (bao gồm test/)

**Files:** không sửa.

- [ ] **Step 1: Chạy typecheck toàn workspace @pms/api (gồm test/)**

Run:
```bash
pnpm --filter @pms/api exec tsc --noEmit
```
Expected: EXIT 0, KHÔNG output lỗi. (`tsconfig` của api include `test/` → bắt lỗi type trong e2e mới.) Nếu đỏ → sửa type ở test vừa thêm (thường do sai kiểu `expect`/thiếu `?`), chạy lại tới xanh.

---

### Task 4: Cập nhật PROGRESS.md (§5 Lịch sử commit)

**Files:**
- Modify: `PROGRESS.md` (§5 "Lịch sử commit", bảng `| Commit | Nội dung |`, chèn ở ĐẦU bảng — dòng ngay dưới header/separator, hiện tại là dòng 159 `| \`75891dd\` | Task 6.5 …`).

**Lý do:** Bảng commit đang dừng ở EPIC 6/task 3.3 — **thiếu toàn bộ Phase 3** (kể cả #57/#58 đã merge). Task 9.4d chỉ chịu trách nhiệm ghi **task của chính phase Wave-1 #4** (9.4a–9.4d). (Việc bổ sung #57/#58 vào PROGRESS = follow-up riêng, KHÔNG ôm vào PR này để giữ scope.)

- [ ] **Step 1: Lấy SHA thật của 3 commit đã có + commit 9.4d sắp tạo**

Run:
```bash
git log --oneline main..HEAD
```
Ghi lại 7-hex của `9.4a` (0162e28), `9.4b` (44d7d84), `9.4c` (2473f03). Commit 9.4d sẽ có SHA mới sau khi commit ở Task 5 — quay lại điền (hoặc gộp: commit code trước rồi cập nhật PROGRESS trong CÙNG commit bằng cách stage PROGRESS sau, xem Task 5).

- [ ] **Step 2: Chèn 4 dòng vào đầu bảng §5 (ngay dưới dòng separator `|---|---|`, TRƯỚC dòng `75891dd`)**

```markdown
| `<SHA-9.4d>` | Task 9.4d (Wave-1 #4) — e2e discounts + quote-with-discount đóng: HK→403 quản lý; đối chiếu đủ 15 mục acceptance; typecheck cuối (gồm test/) xanh |
| `2473f03` | Task 9.4c (Wave-1 #4) — wire discount_code vào quote (FIXED/PERCENT, DISCOUNT line âm, persist discount_code_id, 422 DISCOUNT_NOT_APPLICABLE) + GET /:code/validate (booking.read) + verifyQuoteForBooking add-back |
| `44d7d84` | Task 9.4b (Wave-1 #4) — DiscountsService CRUD + applyDiscount (mọi reason_code, FIXED cap, PERCENT roundVnd) + redeemInTx atomic chống double-spend (migration 0033 quotes.discount_code_id) |
| `0162e28` | Task 9.4a (Wave-1 #4) — migration 0032 discount_codes (FIXED\|PERCENT, RLS, composite FK, soft-delete) + shared-types Zod đồng bộ CHECK |
```

(`<SHA-9.4d>` = SHA commit tạo ở Task 5. Nếu executor commit code+PROGRESS chung một commit thì để placeholder rồi amend, hoặc dùng dòng mô tả không SHA rồi bổ sung — ưu tiên gộp như Task 5 Step 2.)

- [ ] **Step 3: Cập nhật dòng "Cập nhật:" đầu file** (dòng 3) sang ngày hôm nay nếu muốn (tuỳ chọn, không bắt buộc).

---

### Task 5: Commit (1 task = 1 commit) + flag follow-up

**Files:** stage đúng các file đã đụng.

- [ ] **Step 1: Kiểm trạng thái**

Run:
```bash
rtk git status
```
Expected: chỉ `apps/api/test/e2e/discounts.e2e-spec.ts` (+ có thể pricing-quote nếu Task 2 phát sinh) và `PROGRESS.md` thay đổi; thêm file plan `docs/superpowers/plans/2026-07-02-discounts-9.4d-e2e-finalize.md` (untracked).

- [ ] **Step 2: Stage + commit (gộp code + PROGRESS trong 1 commit, rồi điền SHA nếu cần)**

```bash
rtk git add apps/api/test/e2e/discounts.e2e-spec.ts PROGRESS.md docs/superpowers/plans/2026-07-02-discounts-9.4d-e2e-finalize.md
rtk git commit -m "$(cat <<'EOF'
test(discounts): đóng e2e Wave-1 #4 — HK→403 quản lý + đối chiếu đủ acceptance (9.4d)

Bổ sung lỗ hổng e2e còn thiếu so với summary 9.4d và đóng tính năng mã giảm giá.
9.4a/b/c đã dựng migration + service + wire quote + phần lớn e2e; task này VÁ đúng
phần thiếu + verify + typecheck cuối, KHÔNG thêm migration/đổi API.

- e2e discounts: thêm HOUSEKEEPER (KHÔNG rate_plan.manage) → 403 trên MỌI endpoint
  quản lý (create/list/getById/patch/delete). Trước đó chỉ có ACCOUNTANT→403 quản lý
  và HK→403 chỉ ở validate (booking.read). HK là vai thấp nhất → chốt ma trận riêng.
- Đối chiếu đủ 15 mục acceptance 9.4d với spec hiện có (FIXED cap / PERCENT roundVnd /
  min_order / valid window / max_uses cạn / applicable / quote-có-mã total-netted /
  thiếu-code backward-compat / used_count +1 idempotent-retry / cross-tenant RLS 404
  KHÔNG slug-swap). Cleanup afterAll giữ thứ tự FK (quotes/bookings→discount_codes→
  tenants; admin.end rồi app.close cuối).
- Chạy 1 spec sạch (LOG_LEVEL=fatal, KHÔNG rtk vitest) + typecheck cuối gồm test/ xanh.
- PROGRESS.md §5: thêm 9.4a–9.4d.

Follow-up (KHÔNG trong PR này): FE trang quản lý mã (web-admin) + ô nhập mã ở booking
form (gọi GET /:code/validate) + regenerate openapi.json (đang STALE: 0 tham chiếu
discount-codes) + backfill #57/#58 vào PROGRESS §5.

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>
EOF
)"
```

Nếu dùng placeholder `<SHA-9.4d>` trong PROGRESS: sau commit, lấy SHA mới rồi
`rtk git commit --amend` (chỉ khi thực sự muốn SHA khớp; dòng mô tả vẫn hữu ích nếu bỏ SHA).

- [ ] **Step 3: Flag follow-up (ghi vào PR description / trả cho orchestrator)**

Ba follow-up (đã nêu trong commit body):
1. **FE trang quản lý mã** ở web-admin (list/create/edit/soft-delete; components `@pms/ui`, schema `shared-types/discount-code`).
2. **Ô nhập mã ở booking form** — gọi `GET /api/v1/discount-codes/:code/validate?subtotal_vnd&property_id` (gate `booking.read`) để pre-check trước khi báo giá; hiển thị reason_code (BELOW_MIN_ORDER/EXPIRED/…).
3. **Regenerate `docs/openapi.json`** — hiện STALE (0 tham chiếu `discount-codes`; thiếu cả CRUD lẫn `/validate` lẫn `quote.discount_code`). Chạy script gen openapi của repo.
4. (phụ) **Backfill #57 (Landlord R2R) + #58 (Global Person Identity)** vào PROGRESS §5.

---

## Self-Review

**1. Spec coverage:** 15 mục acceptance 9.4d → Task 2 checklist map 1-1; mục duy nhất chưa có (HK→403 quản lý) → Task 1. Typecheck cuối → Task 3. PROGRESS → Task 4. Follow-up → Task 5 Step 3. Đủ.

**2. Placeholder scan:** Chỉ `<SHA-9.4d>` là placeholder có chủ đích (SHA sinh sau commit) — đã ghi cách xử lý. Test code Task 1 là mã đầy đủ, không TODO.

**3. Type consistency:** `dc`, `auth`, `hkToken`, `fixedId`, `http`, `RUN` đều là ký hiệu ĐÃ TỒN TẠI trong spec (dòng 40–51, 226) — không phát minh tên mới. Endpoint paths khớp controller (`/api/v1/discount-codes`, `/:id`, `/:code/validate`). reason_code tên chuẩn: `BELOW_MIN_ORDER` (không phải `MIN_ORDER`), `USAGE_LIMIT_REACHED`, `PROPERTY_NOT_ELIGIBLE`, `NOT_FOUND`, `DISCOUNT_EXHAUSTED`, `DISCOUNT_NOT_APPLICABLE`, `DISCOUNT_CODE_DUPLICATE`, `DISCOUNT_VALUE_INVALID`.
